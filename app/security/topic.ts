export type SecuritySource = "web" | "identity" | "cloud" | "delivery" | "incident" | "ai" | "architecture";
export type SecurityTopic = { title: string; goal: string; mechanism: string; decision: string; failure: string; example: string; question: string; answer: string; wrongA: string; wrongB: string; allow: string; deny: string; controls: [string, string, string]; signal: string; source: SecuritySource };
export const s = (title: string, goal: string, mechanism: string, decision: string, failure: string, example: string, question: string, answer: string, wrongA: string, wrongB: string, allow: string, deny: string, controls: [string, string, string], signal: string, source: SecuritySource): SecurityTopic => ({ title, goal, mechanism, decision, failure, example, question: question.length < 35 ? `In the fictional Relay case about ${title.toLowerCase()}, ${question}` : question, answer, wrongA, wrongB, allow, deny, controls, signal, source });
