"use client";
import type { PlayerProgress } from "../progress";
import { getDataLessons } from "./curriculum";
import { dataCompleted } from "./progress";
import { DATA_PATHS, type DataPaceId } from "./track";

const MATCH: Record<DataPaceId, string> = { beginner: "I am new to data pipelines or want stronger data foundations.", intermediate: "I can transform data and want production workflow and scale skills.", expert: "I design data platforms or own governance, reliability, and architecture." };
export default function DataPacePicker({ progress, ready, recommendation, onRecommend, onSelect }: { progress: PlayerProgress; ready: boolean; recommendation: DataPaceId; onRecommend: (paceId: DataPaceId) => void; onSelect: (paceId: DataPaceId) => void }) {
  return <section className="python-pace-picker cloud-pace-picker backend-pace-picker frontend-pace-picker data-pace-picker" id="data-content">
    <div className="pace-picker-hero"><button onClick={() => window.location.assign("/tracks")}>← All tracks</button><p className="pixel-kicker">DATA GRID · CHOOSE YOUR PATH</p><h1>Choose your<br /><span>Data pace</span></h1><p>Start where you are. Each level has 21 interactive topics across four worlds, and progress is saved separately for every path.</p></div>
    <section className="pace-recommender" aria-labelledby="data-pace-recommender-title"><div><p>PACE FINDER</p><h2 id="data-pace-recommender-title">How familiar are you with data engineering?</h2></div><div>{DATA_PATHS.map((path) => <button className={recommendation === path.id ? "active" : ""} onClick={() => onRecommend(path.id)} key={path.id}><strong>{path.id}</strong><span>{MATCH[path.id]}</span></button>)}</div><p>Recommended path: <strong>{DATA_PATHS.find((path) => path.id === recommendation)?.label}</strong>. You can switch later without losing progress.</p></section>
    <div className="pace-grid">{DATA_PATHS.map((path, index) => { const topics = getDataLessons(path.id); const completed = ready ? dataCompleted(progress, path.id).length : 0; const percent = Math.round(completed / topics.length * 100); return <article className={"pace-card " + path.id + (recommendation === path.id ? " recommended" : "")} key={path.id}><div className="pace-card-art" aria-hidden="true"><span>{index + 1}</span><i /><i /><b>{path.estimatedLevel}</b></div><div className="pace-card-body"><div className={"recommendation-badge " + (recommendation === path.id ? "" : "recommendation-placeholder")} aria-hidden={recommendation !== path.id}>RECOMMENDED START</div><div className="pace-tier"><span>PATH {String(index + 1).padStart(2, "0")}</span><small>{topics.length} TOPICS</small></div><h2>{path.label}</h2><strong>{path.tagline}</strong><p>{path.description}</p><div className="pace-for"><small>RECOMMENDED FOR</small><span>{path.recommendedFor}</span></div><div className="pace-topic-preview">{topics.slice(0, 5).map((lesson) => <span key={lesson.title}>{lesson.title}</span>)}<span>+{topics.length - 5} more</span></div><div className="pace-card-progress"><div role="progressbar" aria-label={path.label + " progress"} aria-valuemin={0} aria-valuemax={topics.length} aria-valuenow={completed}><i style={{ width: percent + "%" }} /></div><span>{ready ? completed + "/" + topics.length + " complete" : "Loading…"}</span></div><button className="pace-card-cta" disabled={!ready} onClick={() => onSelect(path.id)}>{!ready ? "Loading progress…" : completed ? "Continue " + path.label : "Start " + path.label} →</button></div></article>; })}</div>
    <div className="pace-picker-note"><span>◇</span><p><strong>No external data platform is required.</strong>Practice with static SQL, workflow YAML, data contracts, Python transforms, logs, traces, plan diffs, cost reports, and incident timelines entirely in this page.</p></div>
  </section>;
}
