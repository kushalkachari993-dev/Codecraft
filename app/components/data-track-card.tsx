"use client";
import type { PlayerProgress } from "../progress";
import { dataProfileStat } from "../data/progress";
import { DATA_TRACK } from "../data/track";
export default function DataTrackCard({ progress, recommended = false }: { progress: PlayerProgress; recommended?: boolean }) { const completed = dataProfileStat(progress).completed; return <article className={`track-card data ${recommended ? "recommended" : ""}`}><div className="track-art" aria-hidden="true"><span>DE</span><i /><i /></div><div className="track-card-body"><div className={"recommendation-badge " + (recommended ? "" : "recommendation-placeholder")} aria-hidden={!recommended}>RECOMMENDED FOR YOUR GOAL</div><p>DATA GRID</p><h2>Data Engineering</h2><strong>Beginner · Intermediate · Expert</strong><span>Build trustworthy batch pipelines, production data workflows, and governed real-time platforms.</span><div className="track-fit"><small>BEST FIT</small><strong>Data learners, analytics engineers, pipeline developers, and aspiring data-platform architects.</strong></div><div className="realm-signature"><small>REALM MISSION</small><p>Restore reliable movement from raw records to governed data products.</p><b>◆ DATA SIGNAL</b></div><div className="track-skills"><small>Batch pipelines</small><small>Distributed data</small><small>Streaming & governance</small></div><div className="track-card-progress"><div><i style={{ width: `${completed / DATA_TRACK.total * 100}%` }} /></div><span>{completed}/{DATA_TRACK.total} topics</span></div><button onClick={() => window.location.assign("/tracks/data")}>Choose your Data path →</button></div></article>; }
